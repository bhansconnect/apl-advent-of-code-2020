 p2←{
     cnt←{
         (r d)←⍺
         h←⌈(÷d)×⊃⍴⍵
         +⌿'#'=⍵[⊂¨(1+(⊢-⊃)(d×⍳h)),¨1+(2⊃⍴⍵)|r×1-⍨⍳h]
     }
     ⎕←a←1 1 cnt ⍵
     ⎕←b←3 1 cnt ⍵
     ⎕←c←5 1 cnt ⍵
     ⎕←d←7 1 cnt ⍵
     ⎕←e←1 2 cnt ⍵
     ×/a b c d e
 }
